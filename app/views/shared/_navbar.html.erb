<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Hot Vinyl Records</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "Hot Vinyl Store", products_path, class: "nav-link" %>
        </li>
        <% if !user_signed_in? %>
            <li class="nav-item">
                <%= link_to "Sign In", new_user_session_path , class: "nav-link" %>
            </li>
            <li class="nav-item">
                <%= link_to "Sign Up", new_user_registration_path, class: "nav-link" %>
            </li>
        <% else %>
            <% if current_user.seller %>
                <li class="nav-item">
                    <%= link_to 'Sell Vinyl', new_product_path, class: "nav-link" %>
                </li>
                <li class="nav-item">
                    <%= link_to 'My Listed Items', my_selling_products_path, class: "nav-link" %>
                </li>
            <% end %>
            <li class="nav-item">
                <%= link_to "My Orders", orders_index_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
                <%= link_to "Sign Out", destroy_user_session_path, method: 'delete', class: "nav-link" %>
            </li>
        <% end %>
      </ul>
      <%# This is the code for search bar %>
      <%= form_tag(search_path, :method => "get") do %> 
      <%# <form class="d-flex"> %>
        <%= search_field_tag :search, params[:search], placeholder: "Search for Album or Artist", class: "form-control me-2" %>
        <%# <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search"> %>
        <%= button_tag "Search" %>
        <%# <button class="btn btn-outline-success" type="submit">Search</button> %>
      <%# </form> %>
      <% end %>
    </div>
  </div>
</nav>